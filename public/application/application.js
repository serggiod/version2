angular.module('legislaturaweb',
	[
		'ngRoute'
		//addmodule.
	])
	.config(function($routeProvider){
	    $routeProvider
	    	.when('/',{
	    		redirectTo:'/home'
	    	})
			//addroute.
			.otherwise({redirectTo:'/home'});
	});